[build]
builder = "nixpacks"
buildCommand = "pnpm install --no-frozen-lockfile --force && pnpm run build"

[deploy]
startCommand = "pnpm run db:push && node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
