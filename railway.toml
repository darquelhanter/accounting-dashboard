[build]
builder = "nixpacks"
buildCommand = "pnpm install --no-frozen-lockfile --force && pnpm run build && pnpm run db:push"

[deploy]
startCommand = "node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
